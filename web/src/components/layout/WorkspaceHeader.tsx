export type WorkspaceHeaderProps = Record<string, never>

export function WorkspaceHeader(_props: WorkspaceHeaderProps) {
  return (
    <header className="flex justify-end items-center gap-4">
    </header>
  )
}
