all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_become: false
  children:
    gomi_kvm_lab:
      hosts:
        gomi_lab_host:
          ansible_host: 192.168.2.103
          ansible_port: 22
